let reportAndThrowError,stripLeadingSlash,stripTrailingSlash,fetchLogo,fetchPkg,fetchStats;_fa4‍.x([["collectiveSlugFromUrl",()=>collectiveSlugFromUrl],["collectiveUrl",()=>collectiveUrl],["collectiveLogoUrl",()=>collectiveLogoUrl],["collectiveDonationText",()=>collectiveDonationText],["getCollective",()=>getCollective],["collectiveDonationUrl",()=>collectiveDonationUrl],["retrieveDonationSlug",()=>retrieveDonationSlug]]);_fa4‍.w("./misc",[["reportAndThrowError",["reportAndThrowError"],function(v){reportAndThrowError=v}],["stripLeadingSlash",["stripLeadingSlash"],function(v){stripLeadingSlash=v}],["stripTrailingSlash",["stripTrailingSlash"],function(v){stripTrailingSlash=v}]]);_fa4‍.w("./fetch",[["fetchLogo",["fetchLogo"],function(v){fetchLogo=v}],["fetchPkg",["fetchPkg"],function(v){fetchPkg=v}],["fetchStats",["fetchStats"],function(v){fetchStats=v}]]);


       const collectiveSlugFromUrl = url => url.substr(url.lastIndexOf('/') + 1).toLowerCase().replace(/\.json/g, '');_fa4‍.j(["collectiveSlugFromUrl"]);

       const collectiveUrl = pkg => {
  const url = pkg.collective && pkg.collective.url

  if (!url) {
    _fa4‍.a("reportAndThrowError",reportAndThrowError)('No collective URL set!')
  }

  return _fa4‍.a("stripTrailingSlash",stripTrailingSlash)(url)
};_fa4‍.j(["collectiveUrl"]);

// use pkg.collective.logo for "legacy"/compatibility reasons
       const collectiveLogoUrl = pkg => pkg.collective.logo || pkg.collective.logoUrl || false;_fa4‍.j(["collectiveLogoUrl"]);

       const collectiveDonationText = pkg => (pkg.collective.donation && pkg.collective.donation.text) || 'Donate:';_fa4‍.j(["collectiveDonationText"]);

       const getCollective = async pkgPath => {
  const pkg = _fa4‍.a("fetchPkg",fetchPkg)(pkgPath)
  const url = collectiveUrl(pkg)
  const baseCollective = {
    url,
    slug: collectiveSlugFromUrl(url),
    logoUrl: collectiveLogoUrl(pkg),
    donationUrl: collectiveDonationUrl(pkg),
    donationText: collectiveDonationText(pkg)
  }
  const logoUrl = baseCollective.logoUrl
  const promises = [_fa4‍.a("fetchStats",fetchStats)(url)].concat(logoUrl ? _fa4‍.a("fetchLogo",fetchLogo)(logoUrl) : [])

  const [stats, logo] = await Promise.all(promises)

  return Object.assign(baseCollective, { stats, logo })
};_fa4‍.j(["getCollective"]);

       const collectiveDonationUrl = pkg => {
  const defaultDonationAmount = pkg.collective.donation && pkg.collective.donation.amount

  let donateUrl = `${collectiveUrl(pkg)}/${retrieveDonationSlug(pkg)}`

  if (defaultDonationAmount) {
    return `${donateUrl}/${defaultDonationAmount}`
  }

  return donateUrl
};_fa4‍.j(["collectiveDonationUrl"]);

       const retrieveDonationSlug = pkg => {
  const rawDonationSlug = (pkg.collective.donation && pkg.collective.donation.slug)

  if (!rawDonationSlug) {
    return 'donate'
  }

  return _fa4‍.a("stripLeadingSlash",stripLeadingSlash)(rawDonationSlug)
};_fa4‍.j(["retrieveDonationSlug"]);
